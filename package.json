{
  "name": "directory_scanner",
  "version": "1.0.0",
  "description": "A directory scanner that recursively displays the files in a directory",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.0.12",
    "jest": "^30.0.4",
    "typescript": "^5.8.3"
  }
}
